# Migrations

Alembic foi inicializado para usar a URL do banco a partir das configurações do app.

Comandos comuns:

```bash
uv run alembic current
uv run alembic revision -m "init"
uv run alembic upgrade head
```
